services:
  oracle_deploy:
    build:
      context: .
    image: pifsc/oracle_deploy:latest
    container_name: oracle_deploy
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "1"
