# define the database connection information for the database instances and schemas that are used to deploy/upgrade Oracle DBs/APEX apps
ORACLE_DB_HOST='[HOSTNAME]/[SERVICE_NAME]'

# data schema credentials
ORACLE_DB_USER='[DB_SCHEMA_NAME]'
ORACLE_DB_PASS='[DB_PASSWORD]'

# app schema credentials
ORACLE_DB_APP_USER='[DB_APP_SCHEMA_NAME]'
ORACLE_DB_APP_PASS='[DB_APP_PASSWORD]'
